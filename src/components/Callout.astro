---
// Usage in MDX (keep blank lines around the inner text so MDX wraps it in <p>):
//   <Callout type="blue">
//
//   🔑 ...text...
//
//   </Callout>
const { type = "" } = Astro.props;
---

<div class={`blog-callout ${type}`.trim()}>
  <slot />
</div>
