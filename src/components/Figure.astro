---
const { src, alt = "", caption = "", narrow = false } = Astro.props;
---

<div class={`blog-figure${narrow ? " narrow" : ""}`}>
  <img src={src} alt={alt} loading="lazy" decoding="async" />
  {caption && <figcaption set:html={caption} />}
</div>
